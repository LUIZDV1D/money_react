import React from 'react';
import ReactLoading from 'react-loading';
 
const LoaderPagos = () => (
    <ReactLoading type={'spokes'} color={'green'} height={30} width={30} />
);
 
export default LoaderPagos;